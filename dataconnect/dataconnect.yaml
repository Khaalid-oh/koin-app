specVersion: "v1beta"
serviceId: "koin-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "koin-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
